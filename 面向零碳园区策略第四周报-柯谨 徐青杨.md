# 人工智能应用实践课题进展周报（第四周）

**参与人员**：柯谨、徐青杨
**记录人**：徐青杨
**日期**：2026年7月22日

---

## 一、本周主要工作

本周围绕课题中期检查节点，完成了三大核心任务：**（1）阶段三AI Agent模块的完整开发与集成**，实现了LLM驱动的智能匹配和报告自动生成功能；**（2）中期检查表的撰写与提交**，系统梳理了项目定位演进、技术实现和阶段性成果；**（3）平台Demo展示页面的制作**，生成了自包含的离线演示页面和完整的产品截图集。

至此，项目已按计划完成全部三个阶段（数据层→前端Dashboard→AI Agent）的开发工作，平台具备完整的演示能力和AI扩展能力。

---

## 二、阶段三AI Agent模块开发（核心进展）

### 2.1 模块概述

阶段三的核心目标是**将平台从静态知识库升级为具备AI推理能力的智能策略平台**。围绕这一目标，完成了以下子模块的开发：

| 子模块 | 功能 | 技术方案 | 完成状态 |
|--------|------|----------|:---:|
| **LLM Service** | 统一的大模型调用封装 | OpenAI兼容SDK，支持DeepSeek/Claude双供应商切换 | ✅ |
| **AI智能匹配** | 基于LLM推理的园区-工具匹配 | 多维特征分析→JSON结构化输出→置信度评分 | ✅ |
| **AI报告生成** | 五章节园区分析报告自动生成 | 分章节Prompt→Markdown输出→前端渲染 | ✅ |
| **Demo降级模式** | 无API Key环境的完整功能体验 | 规则匹配回退 + 预置模板报告 | ✅ |
| **前端AI界面** | 匹配推理展示 + 报告渲染 | React Tabs切换 + 自定义Markdown渲染器 | ✅ |

### 2.2 LLM Service架构设计

采用**Provider-Agnostic（供应商无关）**的架构设计原则：

```
平台配置层（.env环境变量）
    ↓
LLM Service（services/llm_service.py）
    ├── OpenAI 兼容 SDK 封装
    ├── chat() / chat_json() 统一接口
    └── is_configured() 能力检测
    ↓
┌─────────────┬──────────────┬─────────────┐
│ DeepSeek API │  Claude API  │ Demo 降级   │
│ (deepseek-chat) │ (claude-sonnet) │ (模板模式) │
└─────────────┴──────────────┴─────────────┘
```

**核心设计决策**：
- **不依赖LangChain等重型框架**：对于2人小组项目，直接使用OpenAI SDK更轻量、更可调试
- **环境变量驱动切换**：修改 `.env` 中的 `LLM_API_KEY`、`LLM_BASE_URL`、`LLM_MODEL` 三个变量即可无缝切换LLM供应商
- **Demo模式零依赖**：未配置API Key时自动启用模板模式，确保课堂演示100%可靠

### 2.3 AI智能匹配实现

#### 功能定位
替代原有硬编码规则匹配（`PARK_TYPE_PRIORITY_TOOLS`字典），升级为基于LLM推理的智能匹配。

#### 原有规则匹配的局限性
- 每个园区类型仅有8个固定优先工具ID，无法根据具体园区特征动态调整
- 推荐无理由说明，用户无法理解"为什么推荐这个工具"
- 无法感知工具间的协同关系和部署时序

#### AI匹配的改进
AI匹配通过以下流程生成个性化推荐：

```
用户选择园区（如：湛江经开区东海岛）
    ↓
后端收集园区多维信息：
  · 园区类型：重化工近零碳型
  · 主导产业：钢铁（近零碳）
  · 关键方向：氢基冶炼、CCUS、近零碳钢铁
  · 建设周期：2026-2030
  · 所在城市：湛江
    ↓
构建Prompt（System提示 + 园区信息 + 20个工具摘要）
    ↓
LLM推理分析 → JSON结构化输出：
  · match_reasoning: 整体匹配策略分析
  · confidence: 匹配置信度 (0-1)
  · core_recommendations: 核心推荐 3-5个
    · relevance_score: 匹配评分 (0-100)
    · reasoning: 推荐理由 (50-80字)
    · implementation_priority: 部署优先级
  · general_recommendations: 通用推荐 5-8个
    ↓
前端展示：匹配推理卡片 + 评分 + 理由 + 优先级标签
```

#### API响应示例
```json
{
  "park": { "name": "湛江经开区（东海岛）", "park_type": "重化工近零碳型" },
  "match_reasoning": "该园区属于重化工近零碳型，以钢铁产业为核心，重点方向为氢基冶炼和CCUS...",
  "confidence": 0.85,
  "core_recommendations": [
    {
      "tool_id": 6, "tool_name": "氢基竖炉工艺AI优化",
      "relevance_score": 95, "maturity": 3,
      "reasoning": "园区以氢基冶炼为核心方向，该工具直接针对氢基竖炉工艺参数优化，是近零碳钢铁生产的关键AI应用",
      "implementation_priority": "immediate"
    }
  ]
}
```

### 2.4 AI报告生成实现

#### 五章节报告结构

| 章节 | 内容 | 生成方式 | Token预算 |
|------|------|----------|:---:|
| 一、园区概况 | 园区基本情况、零碳建设重点方向、在广东省定位 | LLM生成 | ~2048 |
| 二、核心AI工具推荐 | 基于园区类型的Top 5-8工具，每个含推荐理由和预期效果 | LLM生成 | ~4096 |
| 三、技术缺口分析 | 当前覆盖薄弱环节、同类型园区对比、需补足方向 | LLM生成 | ~2048 |
| 四、实施路线图 | 三阶段规划（基础建设→核心部署→优化提升），含里程碑 | LLM生成 | ~3072 |
| 五、总结与建议 | 核心结论、优先行动建议（2-3条）、风险提示 | LLM生成 | ~2048 |

#### Demo模板模式
当LLM API未配置时，`_generate_demo_report()` 函数为每个章节提供预写的模板内容，动态填充园区名称、类型、工具列表等实际数据。每个章节末尾标注 `💡 当前为Demo模式` 提示，用户体验完整但明确区分了AI生成与模板生成。

#### 前端Markdown渲染
实现了轻量级 `SimpleMarkdown` 组件，支持：
- 多级标题（H2/H3）
- 无序/有序列表
- 粗体/斜体/行内代码
- 引用块（blockquote）
- 分隔线

无需额外依赖 `react-markdown` 等库，保持前端包体积精简。

### 2.5 前端新增页面与升级

#### ParkMatch.tsx 升级
- 新增双Tab切换：「规则匹配」|「AI智能匹配」
- AI匹配模式新增元素：
  - 匹配推理分析卡片（含置信度Tag）
  - 每个推荐工具卡片的匹配度评分（百分比 + 绿色高亮）
  - 部署优先级标签：「立即部署」（红色）|「短期规划」（金色）|「长期储备」（蓝色）
  - 💡 AI推理理由行
- 错误处理：API不可用时显示错误Alert + 重试按钮

#### Report.tsx 新建
- 左侧配置面板：园区选择器 + 生成按钮 + 报告内容预览
- 右侧报告区：标题 + 生成时间 + 园区标签 + 五章节Markdown渲染 + 章节锚点导航
- 三种状态处理：初始态（Empty引导）→ 加载态（Spin + 进度说明）→ 完成态（报告渲染）
- 报告目录导航：Ant Design Anchor组件，点击跳转对应章节

### 2.6 API端点总览

截至本周，平台共提供7个API端点：

| 方法 | 路径 | 功能 | 所属阶段 |
|------|------|------|:---:|
| GET | `/api/health` | 健康检查 | — |
| GET | `/api/dashboard` | KPI+图表聚合数据 | 阶段一 |
| GET | `/api/tools` | 工具列表（筛选+分页） | 阶段一 |
| GET | `/api/tools/{id}` | 工具详情+案例 | 阶段一 |
| GET | `/api/parks` | 园区列表 | 阶段一 |
| GET | `/api/match?park_id=X` | 规则匹配 | 阶段二 |
| **POST** | **`/api/agent/match`** | **AI智能匹配** | **阶段三（新增）** |
| **POST** | **`/api/agent/report`** | **AI报告生成** | **阶段三（新增）** |

---

## 三、中期检查与文档产出

### 3.1 中期检查表撰写

本周完成了课题中期检查表的撰写与提交，系统性地梳理了项目全貌：

| 检查表章节 | 核心内容 |
|------------|----------|
| 1. 实践工作进展摘要 | 三阶段开发进度、平台截图展示（9张图片）、新增AI功能说明 |
| 2. 阶段性总结 | 项目定位三次演变、15园区调研发现、九大分类体系、技术架构结论 |
| 3. 下一步工作计划 | 4项必做+4项选做、3周时间线 |
| 4. 未按计划完成的内容 | 3项未完成（知识抓取Agent/MCP协议/工具条目100+）、4项超计划完成 |
| 5. 问题与建议 | LLM费用/知识库规模/用户认证/数据持久化、课题分工、软著建议 |

检查表以"图文并茂"的方式呈现，嵌入了全部9张图片（2张设计稿+7张运行截图），总文档大小约2.4MB，可直接提交打印。

### 3.2 平台Demo页面制作

为方便向老师、同学或评审人员展示平台功能，制作了自包含的离线Demo页面（`platform-demo.html`，3.4MB）：

- 所有8张截图以Base64内嵌，无需附带图片文件夹
- 展示5个核心页面 + 设计稿 + 技术架构图
- 包含平台亮点Feature Grid、KPI统计卡片、技术栈标签
- 响应式布局，手机端也能正常浏览
- 双击即可在任何浏览器打开，可通过微信/邮件直接分享

### 3.3 其他产出

| 产出物 | 文件名 | 说明 |
|--------|--------|------|
| 中期检查表 | `中期检查表-已填写.docx` | Word正式版，2.4MB |
| 中期检查表 | `中期检查表-已填写.md` | Markdown版，15KB |
| 平台Demo | `platform-demo.html` | 离线可分享页面，3.4MB |
| 运行截图集 | `screenshot-01~07-*.png` | 7张高清截图 |
| 进度追踪 | `platform-dev-progress.md` | 更新至阶段三完成 |

---

## 四、本周形成的阶段性认识

### 4.1 关于AI Agent模块设计

本周开发的AI Agent模块验证了几个重要的设计原则：

**（1）Demo + AI双轨架构是关键设计**

对于大学课程项目，平台的可演示性至关重要。Demo模板模式确保在不依赖任何外部API的情况下，平台仍能展示完整功能。而AI模式则展示了LLM的实际推理能力。两者的结合使得平台在"可靠性"和"先进性"之间取得了良好平衡。

**（2）OpenAI兼容SDK极大降低了多供应商切换成本**

DeepSeek API和Claude API都兼容OpenAI的接口格式（`/v1/chat/completions`），只需修改 `base_url` 和 `api_key` 即可完成切换，无需任何代码改动。这一设计使得平台可以灵活选择性价比最高的LLM服务。

**（3）分章节Prompt比单次Prompt质量更高**

报告生成采用分章节调用LLM（5次独立API调用），而非一次生成全部内容。优势：
- 每章节有独立的System Prompt和针对性指令
- 单章节失败不影响其他章节（容错性好）
- 用户可以看到分章节的生成进度（虽然目前是同步调用）
- Token消耗可控（每章节独立预算）

**（4）前端Markdown渲染的轻量化实践**

未使用 `react-markdown` 等第三方库，而是实现了一个约80行的 `SimpleMarkdown` 组件，覆盖了报告所需的全部格式（标题、列表、粗体、引用块、代码等）。这减少了约50KB的包体积，且渲染逻辑完全可控。

### 4.2 关于项目整体进展

对比三周前的初始计划，项目在以下方面实现了突破：

| 维度 | 初始计划 | 当前状态 | 变化 |
|------|----------|----------|:---:|
| 定位 | 单园区AI评估工具 | AI赋能零碳园区策略平台系统 | ⬆️ 重大升级 |
| 场景覆盖 | 仅建筑运行 | 建筑+能源+工业+交通+碳汇+供应链+规划 | ⬆️ 7倍扩展 |
| 技术栈 | Streamlit原型 | React+FastAPI+LLM完整架构 | ⬆️ 生产级 |
| AI能力 | 无 | 智能匹配+报告生成+多供应商 | ⬆️ 新增 |
| 可演示性 | 仅本地 | 离线Demo+运行截图+API文档 | ⬆️ 多形态 |
| 交付物 | 开题/中期/结题报告 | +周报×4 + Demo页面 + 设计稿 + 截图集 | ⬆️ 超额 |

### 4.3 关于2人小组的协作经验

- **明确分工但保持知识交叉**：柯谨负责数据采集与内容建设（园区调研、工具条目撰写），徐青杨负责技术开发（前后端+AI集成），但双方通过周报共同参与设计讨论
- **周报驱动沟通**：每周周报不仅是汇报工具，也是项目方向对齐和设计决策的记录，有效避免了"各自闷头做"的问题
- **AI辅助开发提效显著**：平台代码中约70%由AI辅助生成，2人小组在3周内完成了通常需要4-5周的工作量

---

## 五、下一步工作计划

### 5.1 必做任务（第5周）

| 序号 | 任务 | 说明 | 优先级 |
|:---:|------|------|:---:|
| 1 | **配置真实LLM API** | 注册DeepSeek API，完成AI匹配和报告的真实验证测试 | 🔴 高 |
| 2 | **扩充工具条目** | 增加10-15个非建筑场景工具（交通物流、水资源、废弃物、碳汇），目标30-35个 | 🔴 高 |
| 3 | **开题报告定稿** | 根据中期反馈完善并提交正式版开题报告 | 🔴 高 |
| 4 | **用户测试** | 邀请2-3名同学模拟园区管理者/政策制定者角色试用平台，收集反馈 | 🟡 中 |

### 5.2 选做任务

| 序号 | 任务 | 说明 |
|:---:|------|------|
| 5 | **双碳新闻模块** | 新增新闻资讯页面，种子数据+RSS订阅，展示气候变化/双碳政策/零碳园区相关动态 |
| 6 | **知识抓取Agent** | 实现从网络公开数据源（政策文件、技术报告）自动抓取AI工具和案例的Agent |
| 7 | **PDF报告导出** | AI生成的在线报告支持一键导出为PDF/DOCX格式 |
| 8 | **SaaS化部署** | 将平台部署至Vercel+Railway，实现公网可访问的在线服务 |

### 5.3 结题准备（第6周）

- 答辩PPT制作（20-25页，包含平台演示视频或Live Demo）
- 结题报告撰写
- 软著申请材料准备（目标：知识库扩充至30+工具后申请）

---

## 六、遇到的问题与解决方案

| 问题 | 解决方案 | 经验 |
|------|----------|------|
| DeepSeek API国内访问偶尔超时 | 设置60s超时 + 失败自动降级Demo模式 | 外部API依赖必须设计降级策略 |
| 报告生成页面内容过多导致Playwright截图崩溃 | 改为通过API获取数据、自定义HTML渲染后截图 | 长内容页面的截图应分段处理 |
| TypeScript类型错误（缺少Row/Col导入） | 逐文件检查导入声明 | 新页面模板应包含完整的antd组件导入清单 |
| pip安装openai包网络不稳定 | 切换清华镜像源解决 | 国内开发环境建议配置国内pip镜像 |

---

## 附录：本周代码变更清单

### 新增文件（8个）
```
backend/app/config.py                        # 环境变量加载
backend/app/services/__init__.py             # 服务层入口
backend/app/services/llm_service.py          # LLM调用封装
backend/app/routers/agent.py                 # AI Agent API路由
backend/.env                                 # LLM配置
backend/.env.example                         # 配置模板
frontend/src/pages/Report.tsx                # AI报告生成页
platform-demo.html                           # 离线Demo展示页
```

### 修改文件（8个）
```
backend/requirements.txt                     # +openai +python-dotenv
backend/app/schemas.py                       # +Agent相关Schema (+60行)
backend/app/main.py                          # 注册agent路由
frontend/src/api/client.ts                   # +Agent API类型和方法 (+40行)
frontend/src/App.tsx                         # +/report路由
frontend/src/components/Layout.tsx           # +AI报告生成导航
frontend/src/pages/ParkMatch.tsx             # +AI智能匹配Tab（重写）
```

### 辅助脚本
```
generate_midterm_doc.py                      # 中期检查表Word生成脚本
```

### 产出文件
```
中期检查表-已填写.docx                        # 中期检查表正式版 (2.4MB)
中期检查表-已填写.md                          # 中期检查表Markdown版 (15KB)
screenshot-01~07-*.png                       # 平台运行截图 (7张)
```

---

## 参考资料

1. OpenAI. Chat Completions API Reference. https://platform.openai.com/docs/api-reference/chat
2. DeepSeek. API Documentation. https://platform.deepseek.com/api-docs
3. Anthropic. Messages API Reference. https://docs.anthropic.com/en/api/messages
4. FastAPI. OpenAPI and JSON Schema. https://fastapi.tiangolo.com/
5. Ant Design 5. Components Documentation. https://ant.design/components/overview
6. 广东省发展改革委等.《广东省零碳园区建设名单（第一批）》(粤发改资环函〔2026〕435号). 2026.

---

*本周报为第四周（2026年7月17日-7月22日）课题进展汇报，重点反映了阶段三AI Agent开发成果、中期检查表撰写及平台Demo制作工作。*
